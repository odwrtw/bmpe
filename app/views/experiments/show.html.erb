<p id="notice"><%= notice %></p>

<h2>
  <%= @experiment.name %>
</h2>

<small><%= @experiment.description %></small>

<div>
  <%= render template: 'cameras/_list', locals: {cameras: @experiment.cameras, experiment: @experiment} %>
</div>

<% if user_signed_in? %>
<%= link_to 'Edit', edit_experiment_path(@experiment) %> |
<% end %>
<%= link_to 'Back', experiments_path %>
